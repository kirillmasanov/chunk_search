# 🚀 Быстрый старт

## Минимальная инструкция для запуска демо

### 1. Установка (1 минута)

```bash
# Перейти в backend
cd backend

# Создать виртуальное окружение с uv
uv venv

# Активировать окружение
source .venv/bin/activate  # macOS/Linux
# или
.venv\Scripts\activate     # Windows

# Установить зависимости
uv pip install -r requirements.txt
```

### 2. Настройка (1 минута)

```bash
# Создать .env файл
cp .env.example .env

# Отредактировать .env (добавить свои credentials)
nano .env  # или любой редактор
```

Содержимое `.env`:
```bash
YANDEX_API_KEY=ваш_api_ключ
YANDEX_FOLDER_ID=ваш_folder_id
YANDEX_CLOUD_MODEL=qwen3-235b-a22b-fp8/latest
```

### 3. Запуск (1 команда)

```bash
# Из директории backend с активированным venv
uvicorn main:app --reload
```

### 4. Использование (3 минуты)

1. Откройте http://localhost:8000
2. **Нажмите кнопку** "🚀 Загрузить данные и создать индексы"
3. **Дождитесь** завершения (~2 минуты, смотрите логи в терминале)
4. Выберите **Режим A** (автоматическое чанкование)
5. Введите: `Можно ли работать без интернета?`
6. Нажмите **Найти**
7. Посмотрите результаты
8. Переключитесь на **Режим B** (пользовательские чанки)
9. Введите тот же вопрос
10. Сравните результаты!

💡 **Совет**: Используйте кнопку "🗑️ Удалить индексы" для повторного тестирования

## 🎯 Что сравнивать

| Метрика | Режим A | Режим B |
|---------|---------|---------|
| Score | Ниже | **Выше** ✅ |
| Структура | Вопрос/ответ разорваны | **Вопрос/ответ вместе** ✅ |
| Точность | Средняя | **Высокая** ✅ |

## 📝 Тестовые вопросы

1. `Можно ли работать без интернета?` ← Основной для демо
2. `Какие модели доступны в AI Studio?`
3. `Как создать Vector Store?`
4. `Что такое чанкование документов?`
5. `Какой формат данных для пользовательских чанков?`

## 🐛 Проблемы?

### Ошибка: "YANDEX_API_KEY должны быть установлены"
→ Проверьте файл `.env`

### Ошибка: "Vector Store для режима еще не готов"
→ Нажмите кнопку "🚀 Загрузить данные и создать индексы" и дождитесь завершения

### Порт 8000 занят
→ Используйте другой порт: `uvicorn main:app --reload --port 8001`

### uv не найден
→ Установите uv: `curl -LsSf https://astral.sh/uv/install.sh | sh`

## ✅ Готово!

Теперь у вас работает демо, показывающее преимущества пользовательских чанков.