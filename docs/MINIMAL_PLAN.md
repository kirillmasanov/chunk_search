# Минимальный план для демо

## 🎯 Цель: Простое демо для показа функционала

**Принцип**: Только то, что нужно для демонстрации разницы между автоматическим и пользовательским чанкованием.

## 📦 Минимальная структура

```
chunk_search/
├── backend/
│   ├── main.py              # Один файл - весь backend!
│   ├── requirements.txt     # Минимум зависимостей
│   └── .env                 # Конфигурация
├── frontend/
│   └── index.html           # Один файл - весь frontend!
├── data/
│   ├── faq.txt              # FAQ для режима A
│   └── faq_chunks.jsonl     # FAQ для режима B
└── README.md                # Быстрый старт
```

## 🔧 Backend: Один файл (main.py)

### Минимальные зависимости (requirements.txt)
```txt
fastapi==0.109.0
uvicorn==0.27.0
openai==1.10.0
python-dotenv==1.0.0
python-multipart==0.0.6
```

### Конфигурация (.env)
```bash
YANDEX_API_KEY=your_api_key_here
YANDEX_FOLDER_ID=your_folder_id_here
YANDEX_CLOUD_MODEL=yandexgpt
```

### Основной функционал (main.py)

**3 простых endpoint'а:**

1. **POST /upload-auto** - загрузить файл для автоматического чанкования
   ```python
   # Загружает faq.txt без extra_body
   file = client.files.create(
       file=(filename, file_content, "text/plain"),
       purpose="assistants"
   )
   ```

2. **POST /upload-chunks** - загрузить файл с готовыми чанками
   ```python
   # Загружает faq_chunks.jsonl с extra_body={"format": "chunks"}
   file = client.files.create(
       file=(filename, file_content, "application/jsonlines"),
       purpose="assistants",
       extra_body={"format": "chunks"}
   )
   ```

3. **POST /search** - выполнить поиск
   ```python
   # Использует client.responses.create() с file_search
   response = client.responses.create(
       model=f"gpt://{folder_id}/{model}",
       instructions="Ты — умный ассистент...",
       tools=[{"type": "file_search", "vector_store_ids": [store_id]}],
       input=query
   )
   ```

### Упрощенный flow

```python
# 1. Загрузить файл
file_id = upload_file(file, format=None)  # или format="chunks"

# 2. Создать Vector Store с файлом
vector_store = client.vector_stores.create(
    name="Demo Store",
    file_ids=[file_id]
)

# 3. Дождаться готовности (polling)
while vector_store.status != "completed":
    time.sleep(2)
    vector_store = client.vector_stores.retrieve(vector_store.id)

# 4. Выполнить поиск
response = client.responses.create(
    model=f"gpt://{folder_id}/{model}",
    tools=[{"type": "file_search", "vector_store_ids": [vector_store.id]}],
    input=query
)

# 5. Вернуть результат
return {
    "answer": response.output_text,
    "chunks": response.output[0].results  # file_search_call results
}
```

## 🎨 Frontend: Один HTML файл

### Структура
```html
<!DOCTYPE html>
<html>
<head>
    <title>AI Search Demo</title>
    <style>
        /* Минимальные стили */
    </style>
</head>
<body>
    <h1>AI Search Demo</h1>
    
    <!-- Выбор режима -->
    <div>
        <button id="modeA">Режим A: Автоматическое чанкование</button>
        <button id="modeB">Режим B: Пользовательские чанки</button>
    </div>
    
    <!-- Поиск -->
    <div>
        <input id="query" placeholder="Введите запрос...">
        <button id="search">Найти</button>
    </div>
    
    <!-- Результаты -->
    <div id="results"></div>
    
    <script>
        // Вся логика здесь
        let currentMode = 'A';
        let storeIdA = null;
        let storeIdB = null;
        
        // Инициализация при загрузке
        async function init() {
            // Загрузить файлы и создать индексы
            storeIdA = await createStore('auto');
            storeIdB = await createStore('chunks');
        }
        
        // Поиск
        async function search() {
            const query = document.getElementById('query').value;
            const storeId = currentMode === 'A' ? storeIdA : storeIdB;
            
            const response = await fetch('/search', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({query, store_id: storeId})
            });
            
            const data = await response.json();
            displayResults(data);
        }
        
        init();
    </script>
</body>
</html>
```

## 📊 Данные: 5 вопросов (минимум)

### faq.txt (для режима A)
```
Вопрос: Можно ли работать с Yandex Cloud AI без интернета?
Ответ: Нет, для работы с Yandex Cloud AI Studio требуется постоянное подключение к интернету, так как все вычисления происходят в облаке.

Вопрос: Какие модели доступны в AI Studio?
Ответ: В Yandex Cloud AI Studio доступны модели YandexGPT, YandexGPT-Lite, qwen3-235b и другие.

Вопрос: Как создать Vector Store?
Ответ: Используйте метод create() через OpenAI SDK, укажите имя хранилища и загрузите файлы.

Вопрос: Что такое чанкование документов?
Ответ: Чанкование - это разбиение больших документов на меньшие фрагменты для эффективного поиска.

Вопрос: Какой формат данных для пользовательских чанков?
Ответ: Используется формат JSONL с полями id и body в каждой строке.
```

### faq_chunks.jsonl (для режима B)
```jsonl
{"body": "Вопрос: Можно ли работать с Yandex Cloud AI без интернета?\nОтвет: Нет, для работы с Yandex Cloud AI Studio требуется постоянное подключение к интернету, так как все вычисления происходят в облаке."}
{"body": "Вопрос: Какие модели доступны в AI Studio?\nОтвет: В Yandex Cloud AI Studio доступны модели YandexGPT, YandexGPT-Lite, qwen3-235b и другие."}
{"body": "Вопрос: Как создать Vector Store?\nОтвет: Используйте метод create() через OpenAI SDK, укажите имя хранилища и загрузите файлы."}
{"body": "Вопрос: Что такое чанкование документов?\nОтвет: Чанкование - это разбиение больших документов на меньшие фрагменты для эффективного поиска."}
{"body": "Вопрос: Какой формат данных для пользовательских чанков?\nОтвет: Используется формат JSONL с полями id и body в каждой строке."}
```

## 🚀 Запуск: 3 команды

```bash
# 1. Установить зависимости
cd backend && pip install -r requirements.txt

# 2. Настроить .env
echo "YANDEX_API_KEY=your_key" > .env
echo "YANDEX_FOLDER_ID=your_folder" >> .env
echo "YANDEX_CLOUD_MODEL=yandexgpt" >> .env

# 3. Запустить
uvicorn main:app --reload
```

Frontend откроется автоматически на `http://localhost:8000`

## 🎬 Демо сценарий (2 минуты)

### Подготовка (автоматически при запуске)
1. Загружается `faq.txt` → создается Vector Store A
2. Загружается `faq_chunks.jsonl` → создается Vector Store B
3. Ожидание готовности обоих индексов

### Демонстрация
1. **Режим A** (30 сек)
   - Нажать "Режим A"
   - Ввести: "Можно ли работать без интернета?"
   - Показать: вопрос и ответ могут быть в разных чанках, score ниже

2. **Режим B** (30 сек)
   - Нажать "Режим B"
   - Ввести тот же запрос
   - Показать: вопрос и ответ в одном чанке, score выше

3. **Вывод** (1 мин)
   - Пользовательские чанки = лучшее качество
   - Полный контроль над структурой данных
   - Выше релевантность (score)

## 📝 Ключевые API методы

### 1. Загрузка файла
```python
# Обычный файл (автоматическое чанкование)
file = client.files.create(
    file=(filename, file_content, "text/plain"),
    purpose="assistants"
)

# JSONL с готовыми чанками
file = client.files.create(
    file=(filename, file_content, "application/jsonlines"),
    purpose="assistants",
    extra_body={"format": "chunks"}  # ← Ключевое отличие!
)
```

### 2. Создание Vector Store
```python
vector_store = client.vector_stores.create(
    name="Demo Store",
    file_ids=[file_id],
    expires_after={"anchor": "last_active_at", "days": 1}
)
```

### 3. Ожидание готовности
```python
while True:
    store = client.vector_stores.retrieve(vector_store_id)
    if store.status == "completed":
        break
    time.sleep(2)
```

### 4. Поиск с агентом
```python
response = client.responses.create(
    model=f"gpt://{folder_id}/{model}",
    instructions="Ты — умный ассистент. Ищи информацию в подключенном индексе.",
    tools=[{
        "type": "file_search",
        "vector_store_ids": [vector_store_id]
    }],
    input=query
)

# Результат
answer = response.output_text
chunks = response.output[0].results  # file_search_call results
```

## ✂️ Что НЕ нужно для демо

### Backend
- ❌ Отдельные сервисы (все в main.py)
- ❌ Pydantic модели (используем dict)
- ❌ Множество endpoints (только 3)
- ❌ Сложная обработка ошибок
- ❌ Логирование
- ❌ Тесты
- ❌ Документация API

### Frontend
- ❌ Отдельные JS/CSS файлы
- ❌ Сложные UI компоненты
- ❌ Индикаторы загрузки (опционально)
- ❌ Модальные окна
- ❌ Адаптивная верстка

### Данные
- ❌ 15 вопросов → достаточно 5
- ❌ Сложные формулировки

## 💡 Итоговая оценка

**Структура**: 4 файла  
**Код**: ~200 строк (backend) + ~100 строк (frontend)  
**Время реализации**: 2-3 часа  
**Для демо**: ✅ Идеально

## 🎯 Готовность к реализации

Все необходимое для Code Mode:
- ✅ Точная структура API (из документации)
- ✅ Минимальная архитектура
- ✅ Примеры кода
- ✅ Тестовые данные
- ✅ Сценарий демонстрации

**Следующий шаг**: Переключиться в Code Mode и реализовать минимальный вариант.