# Чеклист разработки AI Search Demo

## ✅ Завершено

- [x] Архитектура проекта (ARCHITECTURE.md)
- [x] Blueprint документация (BLUEPRINT.md)
- [x] API документация (API.md)
- [x] README с описанием проекта
- [x] План реализации (IMPLEMENTATION_PLAN.md)
- [x] Руководство по установке (SETUP_GUIDE.md)

## 📋 Следующие шаги для режима Code

### Фаза 1: Структура проекта и данные

#### 1.1 Создать структуру директорий
```bash
backend/
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── config.py
│   ├── models.py
│   └── services/
│       ├── __init__.py
│       ├── vectorstore.py
│       ├── search.py
│       └── file_handler.py
├── tests/
│   ├── __init__.py
│   ├── test_vectorstore.py
│   ├── test_search.py
│   └── test_file_handler.py
├── requirements.txt
├── requirements-dev.txt
├── .env.example
├── .gitignore
└── pyproject.toml

frontend/
├── index.html
├── css/
│   └── styles.css
└── js/
    ├── app.js
    ├── api.js
    └── ui.js

data/
├── faq_full.txt
└── faq_chunks.jsonl
```

#### 1.2 Создать файлы зависимостей

**backend/requirements.txt**
```
fastapi==0.109.0
uvicorn[standard]==0.27.0
openai==1.10.0
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-multipart==0.0.6
aiofiles==23.2.1
```

**backend/requirements-dev.txt**
```
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
black==24.1.1
flake8==7.0.0
mypy==1.8.0
httpx==0.26.0
```

**backend/.env.example**
```bash
YC_API_KEY=your_api_key_here
YC_FOLDER_ID=your_folder_id_here
YC_AI_STUDIO_BASE_URL=https://api.yandex-cloud.ru/ai-studio/v1
YC_MODEL_URI=gpt://{folder_id}/qwen3-235b-a22b-fp8/latest
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=true
DEFAULT_TOP_K=5
MAX_CHUNK_SIZE=2000
SEARCH_TIMEOUT=30
CORS_ORIGINS=http://localhost:8080,http://localhost:3000
```

**backend/.gitignore**
```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
.venv/
venv/
ENV/
env/

# Environment variables
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Logs
*.log

# OS
.DS_Store
Thumbs.db
```

#### 1.3 Создать синтетические данные FAQ

См. файл IMPLEMENTATION_PLAN.md раздел "2. Синтетические данные FAQ" для полного содержимого:
- `data/faq_full.txt` - 15 вопросов-ответов в текстовом формате
- `data/faq_chunks.jsonl` - те же 15 Q&A в формате JSONL чанков

### Фаза 2: Backend реализация

#### 2.1 Конфигурация (app/config.py)
- Класс Settings с использованием pydantic-settings
- Загрузка переменных окружения из .env
- Валидация конфигурации

#### 2.2 Модели данных (app/models.py)
- VectorStoreCreate, VectorStoreResponse
- FileUploadResponse
- SearchRequest, SearchResponse, ChunkResponse
- CompareRequest
- StatusResponse
- Валидаторы для всех моделей

#### 2.3 Сервисы

**app/services/vectorstore.py**
- `create_store(name)` - создание Vector Store
- `get_store(store_id)` - получение информации
- `delete_store(store_id)` - удаление
- `list_stores(limit)` - список всех stores
- `wait_for_ready(store_id, timeout)` - ожидание готовности

**app/services/file_handler.py**
- `upload_file(file_path, store_id, format)` - загрузка файла
- `validate_jsonl(content)` - валидация JSONL
- `parse_chunks(jsonl_content)` - парсинг чанков

**app/services/search.py**
- `search(query, store_id, top_k)` - основной поиск
- `_get_chunks(query, store_id, top_k)` - получение чанков
- `_generate_answer(query, context)` - генерация ответа

#### 2.4 API endpoints (app/main.py)
- POST `/api/vectorstore/create`
- GET `/api/vectorstore/{store_id}`
- DELETE `/api/vectorstore/{store_id}`
- GET `/api/vectorstore/list`
- POST `/api/files/upload`
- POST `/api/files/upload-chunks`
- GET `/api/files/{file_id}`
- DELETE `/api/files/{file_id}`
- POST `/api/search`
- POST `/api/search/compare`
- GET `/api/health`
- GET `/api/status/{store_id}`

#### 2.5 Обработка ошибок
- Custom exception handlers
- Валидация входных данных
- Логирование ошибок
- Понятные сообщения об ошибках

### Фаза 3: Frontend реализация

#### 3.1 HTML структура (index.html)
- Заголовок и описание
- Переключатель режимов (A/B)
- Секция загрузки файлов
- Секция создания индекса
- Секция поиска
- Секция результатов
- Модальные окна для ошибок

#### 3.2 Стили (css/styles.css)
- Современный, чистый дизайн
- Адаптивная верстка
- Цветовая схема Yandex Cloud
- Анимации и переходы
- Индикаторы загрузки

#### 3.3 API клиент (js/api.js)
```javascript
class APIClient {
    constructor(baseURL)
    async createVectorStore(name)
    async uploadFile(file, storeId, format)
    async getStoreStatus(storeId)
    async search(query, storeId, topK)
    async compareSearch(query, storeIdA, storeIdB)
}
```

#### 3.4 UI компоненты (js/ui.js)
```javascript
class UIManager {
    showLoading(message)
    hideLoading()
    showError(message)
    showSuccess(message)
    updateStatus(status, progress)
    renderSearchResults(results)
    renderChunks(chunks)
}
```

#### 3.5 Основная логика (js/app.js)
- Инициализация приложения
- Обработка переключения режимов
- Загрузка файлов
- Создание индексов
- Выполнение поиска
- Отображение результатов

### Фаза 4: Тестирование

#### 4.1 Unit тесты
- `tests/test_vectorstore.py` - тесты VectorStoreService
- `tests/test_file_handler.py` - тесты FileHandler
- `tests/test_search.py` - тесты SearchService

#### 4.2 Integration тесты
- Полный цикл: создание store → загрузка → поиск
- Тесты с реальным API (опционально)

#### 4.3 E2E тесты
- Тестирование через UI
- Сценарии из приемочных тестов

### Фаза 5: Документация и финализация

#### 5.1 Обновить README
- Добавить скриншоты
- Примеры использования
- Troubleshooting

#### 5.2 Создать примеры
- Примеры запросов к API
- Примеры JSONL файлов
- Jupyter notebook с демо

#### 5.3 Deployment инструкции
- Docker setup
- Production конфигурация
- Мониторинг и логирование

## 🎯 Приоритеты

### Высокий приоритет (MVP)
1. ✅ Архитектура и документация
2. 🔄 Backend API (основные endpoints)
3. 🔄 Интеграция с Yandex Cloud AI Studio
4. 🔄 Frontend UI (базовая функциональность)
5. 🔄 Тестовые данные FAQ

### Средний приоритет
6. Обработка ошибок и валидация
7. Unit тесты
8. Улучшенный UI/UX
9. Сравнение режимов

### Низкий приоритет (Nice to have)
10. E2E тесты
11. Docker setup
12. Дополнительные примеры
13. Jupyter notebook

## 📝 Заметки для разработчика

### Важные моменты при реализации

1. **OpenAI SDK и Yandex Cloud**
   - Убедитесь, что используете правильный base_url
   - Проверьте совместимость API методов
   - Обратите внимание на формат `extra_body={"format": "chunks"}`

2. **Асинхронность**
   - Все сервисы должны быть async
   - Используйте `await` для всех I/O операций
   - Правильно обрабатывайте таймауты

3. **Валидация JSONL**
   - Проверяйте каждую строку отдельно
   - Давайте понятные сообщения об ошибках с номером строки
   - Обрабатывайте пустые строки

4. **Frontend**
   - Используйте async/await для всех API вызовов
   - Показывайте индикаторы загрузки
   - Обрабатывайте все возможные ошибки
   - Делайте polling для статуса индекса

5. **Тестирование**
   - Используйте моки для Yandex Cloud API в unit тестах
   - Создайте фикстуры для тестовых данных
   - Тестируйте edge cases (пустые файлы, невалидный JSONL и т.д.)

## 🚀 Готовность к переходу в Code Mode

Все необходимые документы созданы:
- ✅ Архитектура системы
- ✅ Детальный blueprint
- ✅ API спецификация
- ✅ План реализации с примерами кода
- ✅ Руководство по установке
- ✅ Чеклист разработки

**Следующий шаг**: Переключиться в Code Mode и начать реализацию по этому плану.

Рекомендуемый порядок:
1. Создать структуру проекта и файлы зависимостей
2. Создать тестовые данные FAQ
3. Реализовать backend (config → models → services → main)
4. Реализовать frontend (HTML → CSS → JS)
5. Тестирование и отладка
6. Финальная документация

## 📊 Метрики успеха

- [ ] Backend API работает и проходит все тесты
- [ ] Frontend корректно отображается и работает
- [ ] Оба режима (A и B) функционируют
- [ ] Поиск возвращает релевантные результаты
- [ ] Режим B показывает лучшие результаты чем режим A
- [ ] Документация полная и актуальная
- [ ] Код соответствует best practices
- [ ] Проект готов к демонстрации клиентам